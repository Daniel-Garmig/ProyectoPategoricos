{% extends 'base.html.twig' %}

{% block title %}Tema{% endblock %}

{% block body %}
    <h1>Tema</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tema.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tema.nombre }}</td>
            </tr>
        </tbody>
    </table>
    {% include 'listas.html.twig' with {'temas':elemento,"tipo":tipo} %}


    <a href="{{ path('app_tema_index') }}">back to list</a>

    <a href="{{ path('app_tema_edit', {'id': tema.id}) }}">edit</a>

    {{ include('tema/_delete_form.html.twig') }}
{% endblock %}
