{% extends 'base.html.twig' %}

{% block title %}Tema{% endblock %}

{% block body %}
    <h1>Tema</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Nombre</th>
                <td>{{ tema.nombre }}</td>
            </tr>
        </tbody>
    </table>
    {% include 'listas.html.twig' with {'elementos':elementos,"tipo":tipo} %}
    adios
<ul>
    {%  for temp in arrayTemp  %}
        <li>tema id: {{ temp.tema_id }}</li>
        <li>pategoria id: {{ temp.pategoria_id }}</li>
        <li>json: {{ temp.json }}</li>
    {% endfor %}
</ul>
    <a href="{{ path('app_tema_index') }}">back to list</a>

    <a href="{{ path('app_tema_edit', {'id': tema.id}) }}">edit</a>

    {{ include('tema/_delete_form.html.twig') }}
{% endblock %}
