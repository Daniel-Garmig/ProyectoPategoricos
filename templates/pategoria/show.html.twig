{% extends 'base.html.twig' %}

{% block title %}Pategoria{% endblock %}

{% block body %}
    <h1>Pategoria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pategorium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ pategorium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pategoria_index') }}">back to list</a>

    <a href="{{ path('app_pategoria_edit', {'id': pategorium.id}) }}">edit</a>

    {{ include('pategoria/_delete_form.html.twig') }}
{% endblock %}
